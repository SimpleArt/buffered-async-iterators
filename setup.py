from setuptools import setup

setup(
    description=
        "A simple implementation of buffered async iterators, "
        "allowing more throughput by letting iterators run while "
        "their loop bodied are being executed."
)
